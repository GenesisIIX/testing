# Calculator App

This repository contains a simple command-line calculator written in Python. It supports basic arithmetic operations:

- Addition
- Subtraction
- Multiplication
- Division

Run the CLI directly or import the functions into other modules.

## Running

```
python calculator.py add 2 3
```

## Testing

```
python -m unittest discover -s tests -v
```
